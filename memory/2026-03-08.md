# 2026-03-08

- Запущен и почти полностью завершён проект `projects/spring-greeting-site` (Next.js + Tailwind): одностраничная весенняя открытка с отдельным контент-файлом `content/siteContent.js`, ротацией фраз, декоративными лепестками и множеством визуальных доработок.
- Spring-сайт опубликован и активно дорабатывался через серию коммитов в `kenform/spring-greeting-site`; рабочая ссылка: `https://spring-greeting-site.vercel.app/`.
- В spring-проекте реализованы: glassmorphism-карточка, мягкие фоновые эффекты, particles, shimmer, адаптивность, type/title UX-правки, favicon, динамический tab title через `visibilitychange`, и улучшения производительности (rAF + reduced-motion safeguards).
- Появился устойчивый операционный урок: локальные изменения могут не совпадать с Vercel-деплоем из-за подключения другого репозитория/ветки/root directory.
- По Eragon-сайту обновлены реферальные ссылки Bybit/Bitget в локальных исходниках (`eragon-exchange-site-next`, `eragon-exchange-site`, `eragon-exchange-site-kenform`), но live URL пользователя продолжал показывать `example.com`, что подтвердило рассинхрон Git/Vercel.
- Создана заметка в Obsidian о баге деплоя/ссылок: `Obsidian-Telegram-KB/00_Inbox/2026-03-08__bug-vercel-eragon-links.md`.
- Проведена системная проверка: `userbot.service` и `openclaw-gateway.service` активны, критичных ошибок в логах userbot за 24ч не найдено; дисковое заполнение ~68%, кэши не аварийные.
- Важная инфраструктурная диагностика: Vercel-проект Eragon привязан к репозиторию `kenform/-eragon-exchange-site-next`, который структурно отличается от локального `projects/eragon-exchange-site-next`; из-за этого актуальные правки не попадали в видимый прод.
- Рекомендован безопасный путь синхронизации Eragon: через отдельную ветку (`sync-eragon`) и preview перед merge, чтобы не сломать существующий прод.
- Новое правило обработки voice от пользователя: с 07:00 до 19:00 МСК приоритетно использовать @voice_transcribot; если бот недоступен/не резолвится/ошибка отправки — fallback на локальный Whisper.

## 2026-03-08 17:36 UTC — Telegram pipeline locked to Wyckoff topics-only
- User confirmed final desired mode: forward only from one source group `-1003398914047` and only specific forum topics.
- Final enforced topic IDs in `projects/telegram-pipeline-v1/config.json`:
  - `58829` (мысли Альтадора)
  - `58926` (Чат Ирины ученицы)
  - `58920` (Чат Эвелины; per user-provided ID list)
  - `58866` (Чат Ильи)
  - `58221` (Закрытые стримы от команды)
  - `58567` (Чат психолога + трейды)
- Hard constraints applied:
  - `TRACK_GROUP_ONLY=true`
  - `TOPIC_FILTER_ENABLED=true`
  - `ALLOWED_CHANNELS=[]`, `ALLOWED_CHANNEL_IDS=[]`
  - `INCLUDE_ALLOWED_CHANNELS_WHEN_FOLDER=false`
  - `BACKFILL_ENABLED=false`
  - `DRY_RUN=false`
- Safety requirement re-validated: pipeline code has hard safety guard that refuses startup if target matches any source chat (prevents writes to source chats/topics).
- Added media forwarding support in `projects/telegram-pipeline-v1/bot.py` for Bot API:
  - photo / video / voice / document
  - caption uses formatted payload if text exists, fallback caption used when text is absent.
- Message format preserved and expanded with topic title when available:
  - `[Source] [ChatID] [TopicID] [TopicTitle]` + body.
